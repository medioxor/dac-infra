#cloud-config
autoinstall:
  version: 1
  locale: en_US
  keyboard:
    layout: us
  ssh:
    install-server: true
    allow-pw: true
  packages:
    - net-tools
    - htop
    - qemu-guest-agent
  storage:
    layout:
      name: direct
    swap:
      size: 0
  user-data:
    package_upgrade: true
    timezone: Etc/UTC
    users:
      - name: vagrant
        # password is "vagrant"
        passwd: $y$j9T$b3k35jaEKQsLBmc.G2DP71$bAZ6WOtt6TRCD.vbku4axTISwxp96zMaCGLKEx6KeU5
        groups: [adm, cdrom, dip, plugdev, lxd, sudo]
        lock-passwd: false
        sudo: ALL=(ALL) NOPASSWD:ALL
        shell: /bin/bash